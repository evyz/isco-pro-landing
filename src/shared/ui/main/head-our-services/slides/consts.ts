import clsx from "clsx";

export const baseStyle = clsx(
  "h-full shrink-0 w-full flex items-center justify-center relative bg-[#070707]"
);

export const baseSlideStyles = clsx(
  "w-auto h-[150px] scale-70 opacity-50 transition-all duration-300"
);
